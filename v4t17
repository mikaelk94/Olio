#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector <int> luvut;
    vector <int>::iterator it;

    int x;

    for (x=0; x<100; x++)
    {
        luvut.push_back(x);
    }

    for (it=luvut.begin(); it!=luvut.end(); it++)
    {
        cout << *it << endl;
    }

    return 0;
}
